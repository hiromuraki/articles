# 常用软件 Portable 化

## Windows Terminal

- 下载：<https://github.com/microsoft/terminal>
- 解包，在 `wt.ext` 同级目录下创建名为 `.portable` 的文件
- 启动，此时程序会在自身目录下创建 `settings` 目录用于存储配置信息

## VS Code

- 下载：<https://code.visualstudio.com/Download>，选择 zip 包
- 解包，在 `code.exe` 同级目录下创建名为 `data` 的目录

## 