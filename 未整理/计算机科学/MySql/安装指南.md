## 1.通过包管理器

```bash
sudo apt install mysql-server
```

**设置 root 密码**

直接登录 mysql

```bash
sudo mysql
```

执行 Sql 语句，将 root 用户的密码修改为 new_password

```bash
ALTER USER 'root'@'localhost' IDENTIFIED BY 'new_password';
FLUSH PRIVILEGES;
EXIT;
```
